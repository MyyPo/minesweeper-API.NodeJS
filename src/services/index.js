const getCoords = require("./getCoords");
const createMatrix = require("./createMatrix");
const prepareField = require("./prepareField");
const uncoverCoords = require("./uncoverCoords");
const checkIfWon = require("./checkIfWon");
const checkIfLost = require("./checkIfLost");

module.exports = {
  getCoords,
  createMatrix,
  prepareField,
  uncoverCoords,
  checkIfWon,
  checkIfLost,
};
